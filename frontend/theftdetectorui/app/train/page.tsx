export default function TrainPage() {
  return (
    <div className="space-y-3">
      <h1 className="text-3xl font-bold">Training</h1>
      <p className="rounded-xl border border-white/10 bg-white/5 p-4 text-muted">
        Training workflow UI is recreated as a page shell. Endpoints are available under `/training/*` in the Django backend.
      </p>
    </div>
  );
}
